class technology:
 def addstack(self, interest):
	
 interest = input("Would you like to be Mentor OR Learner")
 
	for i in range(1,m+1):	 
		if interest == MENTOR
	 	def setMentor(self):
	  	addstack = input("Enter area of expertise")
		 addstack.append(i)
	def setAvailableTime(self):
		setAvailableTime = input("Enter availabilty of Time")
 	getMentor(addstack(),setAvailableTime())
	

	else:
	 def setLearner(self)
	 addstack = input("Enter your preferred subject")